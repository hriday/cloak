# Triple DES Lesson — Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development to implement task-by-task.

**Goal:** 7-step 3DES lesson centered on the meet-in-the-middle attack on 2DES.

**Architecture:** New `algorithms/triple-des/` algorithm + `static/algorithms/triple-des/` module (validators, codegen, mitm.js helper). No browser-side DES — Python codegen using `cryptography.hazmat`'s 3DES. Spec: `docs/superpowers/specs/2026-05-24-3des-lesson-design.md`.

**Tech Stack:** Same as existing algorithms.

---

## Task 1: `mitm.js` — toy cipher + MITM search

**Files:** Create `static/algorithms/triple-des/mitm.js` + `tests/mitm.test.js`

- [ ] Create directory: `mkdir -p /Users/hriday/code/enc_algo/static/algorithms/triple-des/tests`
- [ ] Write tests asserting `toyEncrypt(0x42, 0x10, 0x20)` and `mitmFind` finds at least one valid (K1, K2) for known plaintext-ciphertext pair.
- [ ] Implement `toyEncrypt(plaintext, k1, k2) = ((plaintext ^ k1) ^ k2) & 0xFF` and `mitmFind(plaintext, ciphertext)` returning `[[k1, k2], ...]`.
- [ ] Tests pass. Commit `feat(3des): mitm.js — toy 2DES cipher + MITM search`.

## Task 2: Validators + walkthroughs

**Files:** `static/algorithms/triple-des/validators.js` + `tests/validators.test.js`

- [ ] Validators: `mitm_attack` (multi-input k1+k2, hex/decimal), `pick_3des_message`, `info`.
- [ ] `mitm_attack` validator uses `mitmFind` to compute the valid set, accepts any pair in it.
- [ ] `walkthroughs` for `mitm_attack` (3 rungs ending in revealing one of the valid pairs).
- [ ] Tests. Commit `feat(3des): validators + walkthroughs`.

## Task 3: Codegen

**Files:** `static/algorithms/triple-des/codegen.js` + `tests/codegen.test.js`

- [ ] `full_script(state)` emits Python using `cryptography.hazmat.primitives.ciphers.algorithms.TripleDES` with CBC mode + PKCS7 padding (spec has the canonical script).
- [ ] Per-step stubs return `""` (no inline code panel for the lesson).
- [ ] Tests for `full_script` matching expected Python idioms.
- [ ] Commit `feat(3des): codegen.js — Python TripleDES demo`.

## Task 4: Fixtures + fixture-load test

**Files:** `algorithms/triple-des/fixtures.json` + extend `core/tests/test_fixtures.py`

- [ ] Algorithm pk=4, lesson pk=4, steps pks=41-47.
- [ ] intro_template under 200 chars.
- [ ] 7 steps with the 7 slugs from the spec: `intro, single-des-weakness, naive-2des-intro, mitm-attack, 3des-ede, encrypt-with-3des, done`.
- [ ] Validator keys: `info` for steps 1-3, 5, 7; `mitm_attack` for step 4; `pick_3des_message` for step 6.
- [ ] Run loaddata; verify 9 objects installed.
- [ ] Append `test_3des_fixture_loads` to `core/tests/test_fixtures.py` asserting algorithm + lesson + 7 steps + validator keys.
- [ ] Commit `feat(3des): fixtures + fixture-load test`.

## Task 5: Manual smoke

- [ ] Visit `/algorithms/triple-des/learn/why-3des/` in browser.
- [ ] Walk all 7 steps; MITM step accepts a valid pair; codegen renders.
- [ ] If issues surface, fix + commit.

## Verification

- [ ] All tests green.
- [ ] Triple DES card on landing.
- [ ] Lesson walks end-to-end.
- [ ] RSA / Hybrid / AES still work.
