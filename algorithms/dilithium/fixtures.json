[
  {
    "model": "core.algorithm",
    "pk": 25,
    "fields": {
      "slug": "dilithium",
      "name": "Dilithium (ML-DSA)",
      "family": "pq",
      "status": "live",
      "order": 25,
      "intro_template": "## Dilithium (ML-DSA)\n\nPost-quantum signatures, FIPS 204 (Aug 2024). Module-SIS lattice math; survives Shor. Pairs with Kyber as the PQ stack's signature half."
    }
  },
  {
    "model": "core.lesson",
    "pk": 25,
    "fields": {
      "algorithm": 25,
      "slug": "lattice-signatures",
      "title": "Dilithium: post-quantum signatures",
      "order": 1
    }
  },
  {
    "model": "core.step",
    "pk": 251,
    "fields": {
      "lesson": 25, "order": 1, "slug": "intro", "kind": "info",
      "prompt_template": "## Dilithium — what replaces Ed25519 when the quantum computers arrive\n\nThe Kyber lesson covered the post-quantum KEM. This lesson covers its sibling: **Dilithium**, the post-quantum **signature scheme**. NIST published both in August 2024 — Kyber as **FIPS 203** (ML-KEM), Dilithium as **FIPS 204** (**ML-DSA**, Module-Lattice Digital Signature Algorithm). Together they're the lattice half of the post-quantum stack.\n\n### Same family as Kyber, different problem\n\nKyber security rests on **Module-LWE**: given `t = A·s + e` for short `s, e`, recover `s`. Dilithium rests on **Module-SIS**: given a matrix `A`, find a short vector `z` such that `A·z = 0` (or more precisely, find short `z, c` with `A·z = w + c·t` for some target `w`). Both are lattice problems on the same ring `R_q = Z_q[X] / (X^n + 1)`, both are believed to survive Shor's algorithm, and both reduce — in the worst case — to standard lattice problems (closest-vector / shortest-vector in module lattices).\n\nThe practical consequence: if you understood Kyber's polynomial ring math, the toy Dilithium math drops into the same primitives. We'll re-use `polyAdd`, `polyMul`, `matVecMul` from the Kyber toy with no changes — the only new primitive is `polyInfinityNorm` (\"is this polynomial short?\"), which Kyber didn't need.\n\n### What changes from Ed25519\n\n| | Ed25519 | Dilithium (ML-DSA-65) |\n|---|---|---|\n| Public key | 32 bytes | 1,952 bytes (61× larger) |\n| Signature | 64 bytes | 3,309 bytes (52× larger) |\n| Hard problem | Elliptic-curve discrete log | Module-SIS (lattice) |\n| Quantum resistance | Broken by Shor | Survives Shor |\n| Determinism | Yes | No (rejection sampling) |\n\nThe size tax is real: a TLS certificate with a Dilithium signature is ~50× larger than one with Ed25519. The compute cost is similar (Dilithium sign/verify is ~2-3× slower in software, but that's noise compared to the bytes-on-the-wire problem).\n\n### Where it's deployed today\n\n- **Cloudflare** announced experimental ML-DSA support for TLS certificates in 2024.\n- **OpenSSH 9.9** (October 2024) added `mlkem768x25519-sha256` as a hybrid KEX option, and is tracking ML-DSA for host-key signatures.\n- **AWS KMS** added ML-DSA key wrapping in 2024.\n- **CA/Browser Forum** is working through the ballot process to allow ML-DSA in public-trust certificates; expect deployment in 2026-2027 timeframe.\n- **Apple's PQ3** iMessage protocol uses Kyber for KEM; Dilithium-class signatures are on their published roadmap.\n\nIn the next 7 steps you'll build a toy Dilithium from scratch: the Module-SIS problem, then keygen, then the Fiat-Shamir sign loop (with its characteristic rejection sampling), then verify, then an in-browser sign/verify panel, then real ML-DSA parameter sets and the hybrid deployment story.",
      "help_template": "**In plain English:** Dilithium is the signature scheme that pairs with Kyber. NIST-standardised in 2024 as ML-DSA. Same underlying lattice math as Kyber (just a different problem on top of it), survives quantum computers, deployments rolling out now.",
      "validator_key": "info", "codegen_key": "intro"
    }
  },
  {
    "model": "core.step",
    "pk": 252,
    "fields": {
      "lesson": 25, "order": 2, "slug": "the-problem", "kind": "info",
      "prompt_template": "## The problem under Dilithium — Module-SIS\n\n### Why Ed25519 and ECDSA break\n\nEvery classical signature scheme on this site rests on one of two hard problems:\n\n- **Integer factoring** (RSA-PSS) — given `n = p·q`, find `p, q`.\n- **Discrete logarithm on an elliptic curve** (Ed25519, ECDSA) — given `A = s·G`, find `s`.\n\nBoth fall to **Shor's algorithm** on a fault-tolerant quantum computer. Shor's reduces both to polynomial-time problems on quantum hardware: factoring becomes order-finding, discrete log becomes period-finding, both solvable in `O((log N)^3)` quantum operations. A 2048-bit RSA key falls in hours; a 256-bit ECDSA key falls in minutes. The hardware doesn't exist yet at scale, but the harvest-now-decrypt-later threat means *signatures created today must remain trustworthy in 2035-2040* — well within the projected timeline for cryptographically-relevant quantum computers.\n\n### The lattice alternative\n\nDilithium's security rests on **Module-SIS** (Short Integer Solution over module lattices). Stated as a decision problem:\n\n> Given a uniform random matrix `A ∈ R_q^(k×l)`, find a non-zero short vector `z ∈ R_q^l` such that `A·z = 0 mod q`.\n\n\"Short\" means `||z||_∞ ≤ B` for some bound `B` much smaller than `q`. With the lattice viewpoint: the matrix `A` defines a lattice, and we want a short non-zero vector in that lattice. Worst-case lattice problems (closest-vector, shortest-vector in module lattices) reduce to random Module-SIS instances at Dilithium's parameters, and **the best known quantum algorithms don't substantially improve the situation** for either problem. That's the formal hardness argument.\n\n### Where this gets used in Dilithium\n\nA Dilithium signer holds short `s1, s2` and publishes `t = A·s1 + s2`. To sign, they produce a short `z` such that `A·z − c·t` reduces to a value the verifier can hash. An attacker without `s1, s2` who tries to forge a signature faces *exactly* the Module-SIS problem — find a short vector that satisfies the verification equation. Without the lattice's secret \"trapdoor\" structure (which only the signer knows), Module-SIS is hard.\n\n### Dilithium vs SPHINCS+\n\nNIST also standardised **SPHINCS+** (FIPS 205, also Aug 2024) as a hash-based post-quantum signature. The trade-off:\n\n- **Dilithium** — fast, smaller (~3 KB sig), security from Module-SIS. Most deployments will use it as the default.\n- **SPHINCS+** — slow, bigger (~8-50 KB sig depending on parameter set), but security from *nothing but the underlying hash function*. If a future cryptanalysis breaks lattice assumptions entirely, SPHINCS+ still stands.\n\nThe conservative deployment pattern is to *also* support SPHINCS+ as a fallback — same way TLS deployments combine X25519 with ML-KEM-768 (one classical + one PQ) so the connection survives if either breaks.\n\nFor the next 5 steps we focus on Dilithium. A future lesson will walk SPHINCS+.",
      "help_template": "**In plain English:** RSA and Ed25519 fall to Shor's algorithm on a big enough quantum computer. Dilithium rests on Module-SIS — finding short vectors in a lattice — which Shor doesn't help with. SPHINCS+ is the hash-based backup if lattices ever break.",
      "validator_key": "info", "codegen_key": "the_problem"
    }
  },
  {
    "model": "core.step",
    "pk": 253,
    "fields": {
      "lesson": 25, "order": 3, "slug": "keygen", "kind": "info",
      "prompt_template": "## Toy Dilithium keygen — `q = 257`, `n = 4`, `k = l = 2`\n\nThe signer generates a keypair. Four steps, all in `R_257[X] / (X⁴ + 1)` — the same ring as the Kyber toy:\n\n1. **Sample uniform matrix `A`** of shape `k × l = 2 × 2`. Each entry is a polynomial with coefficients uniform in `[0, 257)`.\n2. **Sample small secret `s1`** — `l = 2` polynomials, each coefficient in `{−1, 0, 1}`.\n3. **Sample small secret `s2`** — `k = 2` polynomials, each coefficient in `{−1, 0, 1}`.\n4. **Compute `t = A·s1 + s2 mod 257`** — the public-key polynomial.\n\n**Public key:** `(A, t)`. **Private key:** `(s1, s2)`.\n\n### Two secrets, two roles\n\nUnlike Kyber, Dilithium needs *two* short secret vectors — `s1` and `s2`. They play different roles:\n\n- **`s1` (length `l`)** is mixed into the signature response `z = y + c·s1`. It's what \"signs\" the message — the part of the signature only the signer can produce.\n- **`s2` (length `k`)** is the noise that randomises `t = A·s1 + s2`. It hides `s1` from Eve (who sees `(A, t)` and would otherwise solve `A·s1 = t` by linear algebra). It also shows up in verification as the bounded error term `w − (A·z − c·t) = c·s2`.\n\nBoth must be small for the signature math to close.\n\n### The lesson's frozen keypair\n\nWe seed a deterministic PRNG with `cloak-dilithium-v1` so every visitor sees the same numbers. The page re-derives them at load time; a regression test in `dilithium_demo.test.js` pins them so they can't silently drift.\n\n**Matrix `A`** (each cell is a polynomial `[c₀, c₁, c₂, c₃]`):\n\n```\nA = [ [206, 231, 91, 176]    [15, 142, 226, 3]    ]\n    [ [26, 64, 209, 67]      [222, 173, 207, 219] ]\n```\n\n**Secret `s1`** (coefficients shown in signed form `{−1, 0, 1}`; canonical `[0, 257)` uses `256` for `−1`):\n\n```\ns1[0] = [-1,  1, -1,  1]      (canonical: [256, 1, 256, 1])\ns1[1] = [ 0,  0, -1,  1]      (canonical: [0, 0, 256, 1])\n```\n\n**Secret `s2`**:\n\n```\ns2[0] = [ 0,  0, -1, -1]      (canonical: [0, 0, 256, 256])\ns2[1] = [ 1,  0,  1,  0]      (canonical: [1, 0, 1, 0])\n```\n\n**Public-key polynomial `t = A·s1 + s2 mod 257`** (computed for you — the lesson tests verify this number):\n\n```\nt[0] = [ 76,  94, 253,  19]\nt[1] = [ 87,  89,  93, 153]\n```\n\n### Why `t` looks random to Eve\n\nEve sees `A` and `t`. She wants `s1` (which is what she needs to forge signatures). The problem she faces is **Module-LWE**: recover the short `s1` from the noisy linear equation `t = A·s1 + s2`. Identical to the recovery problem from the Kyber lesson — same hardness, same protection.\n\nThe second secret `s2` is what makes the recovery problem an LWE instance instead of a clean linear system. Without `s2`, `t = A·s1` would be solvable by Gaussian elimination over `R_q` (when `A` is invertible, which it almost always is at random). The small `s2` is the entire trapdoor.",
      "help_template": "**In plain English:** Sample random matrix `A`, two small secrets `s1` (l-vec) and `s2` (k-vec), then publish `t = A·s1 + s2`. Public key `(A, t)`, private key `(s1, s2)`. Same ring as Kyber; just two small secrets instead of one.",
      "validator_key": "info", "codegen_key": "keygen"
    }
  },
  {
    "model": "core.step",
    "pk": 254,
    "fields": {
      "lesson": 25, "order": 4, "slug": "sign-mechanics", "kind": "info",
      "prompt_template": "## Signing — Fiat–Shamir with rejection sampling\n\nDilithium signing has four steps, then a *loop*. Given private key `(s1, s2)` (the matrix `A` is also stored privately for convenience), public-key polynomial `t`, and a message `m`:\n\n```\n1. Sample y ∈ R_q^l with |y|_∞ ≤ GAMMA1   (random short polynomial vector)\n2. Compute w = A·y mod q                  (commitment)\n3. Compute c = H(m, w) ∈ R_q              (challenge — a small polynomial)\n4. Compute z = y + c·s1 mod q             (response)\n5. If |z|_∞ > GAMMA1 − BETA: GOTO 1       ← rejection sampling\n   else: output signature (z, c, w)\n```\n\nThe toy parameters: `GAMMA1 = 40`, `BETA = 4`, so the acceptance threshold is `|z|_∞ ≤ 36`. Real ML-DSA-65 uses `GAMMA1 = 2^19` and `BETA = 196`, but the shape is the same.\n\n### Walking the five steps\n\n- **Step 1** — sample `y`. Each of the `l·n = 8` coefficients is uniformly random in `[−40, 40]`. This is the per-signature randomness; like Ed25519's `r`, but Dilithium *can't* be deterministic — the rejection loop needs fresh randomness on each retry.\n- **Step 2** — compute the commitment `w = A·y`. A matrix-vector multiply in `R_q^k`. `w` is what the challenge will hash; it pins down `y` cryptographically without revealing it.\n- **Step 3** — derive the challenge. Real Dilithium uses **SHAKE-256** with domain separation and produces a sparse ternary polynomial (exactly `τ = 49` non-zero coefficients in `{−1, +1}` for ML-DSA-65). The toy uses SHA-256 and produces a uniformly ternary polynomial: each of `n = 4` coefficients is `(byte[i] % 3) − 1`.\n- **Step 4** — compute the response `z = y + c·s1`. This is the Schnorr-style response from the Σ-protocol that Fiat-Shamir non-interactivises. The masking by `y` hides `c·s1` (and thus `s1` itself) from the verifier.\n- **Step 5** — **the rejection check.** Compute `|z|_∞`. If any coefficient of `z` exceeds `GAMMA1 − BETA = 36`, reject and start over with a fresh `y`.\n\n### Why we reject\n\nThis is the load-bearing security move. Without rejection, the distribution of `z` would leak information about `s1` — specifically, the *correlation* between `z` and `c` would be biased by `s1`. By accepting only `z` values that lie in a centered window of width `2·(GAMMA1 − BETA)`, Dilithium *forces* `z` to be statistically independent of `c·s1` (the masking by `y` succeeds). This is called the **\"abort technique\"** in lattice signature literature.\n\nThe consequence: about half of the `y` samples produce `z` that's *just outside* the window. Those get thrown away. The signer doesn't ship them, doesn't reveal which ones failed — just keeps rolling.\n\n### The rejection rate at our parameters\n\nWith `GAMMA1 = 40`, `BETA = 4`, and `l·n = 8` independent coefficients, each coefficient lies in the acceptance window `[−36, 36]` with probability `(2·36+1)/(2·40+1) = 73/81`. Across all 8 coefficients: `(73/81)^8 ≈ 0.44`. Empirically the toy averages about **40%–50% first-attempt success, 2–3 attempts per signature on average**.\n\nReal ML-DSA-65 has a higher per-attempt rejection rate (~80%) because the abort threshold is tighter relative to the noise; signatures average 4–7 attempts.\n\n### What gets sent\n\nThe toy signature is `(z, c, w)`. Real ML-DSA replaces `w` with a tiny *hint* `h` that lets the verifier recover the high bits of `w` from `(A·z − c·t)`. We'll explain the cancellation that makes that work on the next step.",
      "help_template": "**In plain English:** Sample random short `y`, hash to a challenge `c`, compute response `z = y + c·s1`. If `z` is too big, throw it away and start over. About half the time the first attempt is rejected. Loop until acceptance. That's Dilithium.",
      "validator_key": "info", "codegen_key": "sign_mechanics"
    }
  },
  {
    "model": "core.step",
    "pk": 255,
    "fields": {
      "lesson": 25, "order": 5, "slug": "verify-mechanics", "kind": "info",
      "prompt_template": "## Verifying — the cancellation that makes Dilithium work\n\nGiven message `m`, signature `(z, c, w)`, public key `(A, t)`:\n\n```\n1. Check |z|_∞ ≤ GAMMA1 − BETA            (response is short)\n2. Compute w' = A·z − c·t mod q           (recover the commitment)\n3. Check |w − w'|_∞ ≤ BETA                (algebra closes within noise)\n4. Recompute c' = H(m, w); check c' == c  (challenge integrity)\n```\n\nAll three checks must pass. If any fails, the signature is invalid.\n\n### Why it works — the cancellation\n\nLet's substitute the definitions of `z` and `t` into `A·z − c·t`:\n\n```\nA·z − c·t\n  = A·(y + c·s1) − c·(A·s1 + s2)            [defs of z and t]\n  = A·y + c·A·s1 − c·A·s1 − c·s2            [distribute]\n  = A·y − c·s2                              [the c·A·s1 terms cancel]\n  = w − c·s2                                [w = A·y by def]\n```\n\nThe `c·A·s1` terms cancel exactly — no noise, no rounding, just algebra. What remains is `w − c·s2`, where `c·s2` is the *noise term*. Both `c` and `s2` are short polynomials (coefficients in `{−1, 0, 1}` for the toy), so their product has small coefficients.\n\n### The noise budget\n\nFor a polynomial product `c · s2` in `R_q[X] / (X^n + 1)` where both have coefficients in `{−1, 0, 1}`:\n\n```\n|c · s2|_∞ ≤ n      (worst case — sum of n terms, each ±1·±1)\n          = 4      (n = 4 for the toy)\n```\n\nSo `|w − w'|_∞ = |c · s2|_∞ ≤ 4 = BETA`. The verifier's check `|w − w'|_∞ ≤ BETA` always passes for legitimately-produced signatures.\n\nIn practice it's almost always smaller — the average `|c·s2|_∞` across many signatures is closer to 2 than 4. The bound 4 is the *worst case* across all possible `c` and `s2` values.\n\n### Why all three checks are needed\n\n- **Check 1 (`|z|_∞` short)** — without this, an attacker could pick any `z` they like and craft `c` to make the algebra work. The shortness check forces `z` to be in the same distribution that the signer's rejection loop produces.\n- **Check 3 (`|w − w'|_∞ ≤ BETA`)** — without this, the verifier wouldn't catch a forgery where `w` was tampered with directly. Bounding the gap to `BETA` means `w` must be the *actual* commitment from a legitimate sign operation.\n- **Check 4 (`c' == c`)** — without this, the signer's choice of `c` would be unconstrained. Hashing `(m, w)` binds the challenge to both the message and the commitment, which is what makes the Fiat-Shamir transform secure.\n\nAll three together provide **EUF-CMA** security (existential unforgeability under chosen-message attacks) — the standard signature security definition.\n\n### Real Dilithium ships a hint, not w\n\nThe toy ships `(z, c, w)`. `w` is `k · n` coefficients = 8 integers = ~16 bytes for the toy. At real ML-DSA-65 sizes `w` is `k · n · log₂(q)` = `6 · 256 · 23` bits ≈ 4.4 KB — way too much to put in every signature.\n\nReal ML-DSA replaces `w` with a *hint* `h`: a sparse boolean vector that tells the verifier which coefficients of `A·z − c·t` round to a different bucket than `w` did. The hint takes ~70 bytes (vs `w`'s 4.4 KB) and lets the verifier recover `w`'s high bits exactly. The math is identical to the toy after the high-bits rounding — just bytes-efficient.\n\nWe ship `w` directly so you can see the cancellation work without the rounding machinery.",
      "help_template": "**In plain English:** Verify by computing `A·z − c·t`. Substituting `z = y + c·s1` and `t = A·s1 + s2`, the big `c·A·s1` terms cancel and you're left with `w − c·s2` — `w` plus a tiny `c·s2` noise term bounded by 4. So `w' ≈ w` and the verifier re-hashes to confirm. Real Dilithium ships a tiny hint instead of `w` to save bytes.",
      "validator_key": "info", "codegen_key": "verify_mechanics"
    }
  },
  {
    "model": "core.step",
    "pk": 256,
    "fields": {
      "lesson": 25, "order": 6, "slug": "sign-and-verify", "kind": "input-text",
      "prompt_template": "### Try toy Dilithium — sign and verify in the browser\n\nThis page generated a Dilithium keypair from the canonical seed `cloak-dilithium-v1`. The same matrix `A`, secrets `s1, s2`, and public `t` that you saw on step 3 are loaded into memory. Sign and verify run against that frozen keypair.\n\n**Try it:**\n1. Pick **Sign**, type a message, and submit. The page runs the Fiat-Shamir loop with rejection sampling, then returns an 80-hex-char signature carrying `(z, c, w)`.\n2. Pick **Verify**, paste the same message + signature, and submit. Should return *valid*.\n3. Edit one hex character of the signature, verify again. Should return *invalid*.\n4. **Sign the same message twice** — you'll get **different bytes** each time. Unlike Ed25519, Dilithium is randomized (the rejection loop draws fresh `y` per attempt). That's intentional and OK security-wise; it's a different design philosophy.\n\n### What's in the 80 hex chars\n\nThe toy signature serialises three components:\n\n```\nz:  l·n = 8 coefficients × 2 bytes each = 16 bytes  =  32 hex chars\nc:  n   = 4 coefficients × 2 bytes each = 8 bytes   =  16 hex chars\nw:  k·n = 8 coefficients × 2 bytes each = 16 bytes  =  32 hex chars\nTotal: 40 bytes = 80 hex chars\n```\n\nReal ML-DSA-65 signatures are **3,309 bytes** (6,618 hex chars). The toy is ~80× smaller because every parameter is dialled down to teaching scale.\n\n### Watch the rejection-sampling counter\n\nThe page reports how many attempts the loop took before acceptance. At our toy parameters (`GAMMA1 = 40`, `BETA = 4`) the first attempt succeeds about 40% of the time; about 30% of signatures need 2 attempts; ~15% need 3; etc. The geometric distribution falls off quickly — 99%+ of signatures complete within 7 attempts.\n\nIf you see \"attempts: 8+\" you've hit the long tail of the distribution. That's expected. The implementation bounds total attempts at 20 with a hard failure beyond that — at expected success rate `p ≈ 0.4`, the probability of needing more than 20 attempts is `(1−p)^20 ≈ 0.00004` (4 in 100,000).",
      "help_template": "**In plain English:** Type, click Sign — the loop draws random `y`, computes `z`, checks if it's short enough, retries until acceptance. The output is 80 hex chars carrying `(z, c, w)`. Verify recomputes `A·z − c·t`, checks the noise term is small, re-hashes for the challenge. Refresh the page and you get the same frozen keypair — same public `t`, same private `s1, s2`, but every new sign produces fresh bytes thanks to the random `y`.",
      "validator_key": "dilithium_operation", "codegen_key": "sign_and_verify"
    }
  },
  {
    "model": "core.step",
    "pk": 257,
    "fields": {
      "lesson": 25, "order": 7, "slug": "real-dilithium-and-deployment", "kind": "info",
      "prompt_template": "## Real Dilithium — three parameter sets and the hybrid deployment\n\nThe lesson walked the toy at `q = 257, n = 4, k = l = 2`. Real Dilithium (standardised as **ML-DSA** in FIPS 204, August 2024) fixes `q = 8380417` (`= 2^23 − 2^13 + 1`, a 23-bit prime chosen for the NTT) and `n = 256`, varying `(k, l)` to hit three security levels:\n\n| Variant | `(k, l)` | NIST security level | Public key | Signature |\n|---|---|---|---|---|\n| **ML-DSA-44** (Dilithium-2) | (4, 4) | ~AES-128 | 1,312 bytes | 2,420 bytes |\n| **ML-DSA-65** (Dilithium-3) | (6, 5) | ~AES-192 | 1,952 bytes | 3,309 bytes |\n| **ML-DSA-87** (Dilithium-5) | (8, 7) | ~AES-256 | 2,592 bytes | 4,627 bytes |\n\n**ML-DSA-65** is the default for most deployments — same security target as ML-KEM-768.\n\n### Compare to Ed25519\n\n| | Ed25519 | ML-DSA-44 | ML-DSA-65 | ML-DSA-87 |\n|---|---|---|---|---|\n| Public key | 32 B | 1,312 B (41×) | 1,952 B (61×) | 2,592 B (81×) |\n| Signature | 64 B | 2,420 B (38×) | 3,309 B (52×) | 4,627 B (72×) |\n\nThat's the **size tax of post-quantum signatures**. ~40-80× larger keys, ~40-70× larger signatures. For a TLS certificate chain (typically 3–5 certs), the difference is real wire bytes — `Compression` of Dilithium signatures is an active research topic, but at the current state of the art there's no way to shrink them substantially while keeping the post-quantum security guarantee.\n\n### What changes from the toy\n\nThree structural shifts:\n\n1. **Larger ring.** `R_8380417[X] / (X^256 + 1)`. Degree-256 polynomials. Each coefficient takes 23 bits.\n2. **Sparse fixed-weight challenge.** Real Dilithium's `c` is a polynomial with exactly `τ` non-zero coefficients in `{−1, +1}`, rest zero. ML-DSA-65 uses `τ = 49`. The sparsity bounds `|c·s|_∞` more tightly, which lets `BETA` be small relative to `GAMMA1`, which keeps the rejection rate manageable. The toy uses uniformly-ternary `c` for simplicity.\n3. **Hint-based signatures.** Instead of shipping `w` directly (~4 KB at real parameters), real ML-DSA ships a sparse boolean `hint` vector (~70 bytes) that lets the verifier recover the high bits of `w` from `(A·z − c·t)`. The math is the same after high-bits rounding; the bytes saved are the entire reason the hint machinery exists.\n4. **NTT-based polynomial multiplication.** Schoolbook `O(n²)` is fine at `n = 4`; at `n = 256` we need the **Number-Theoretic Transform** for `O(n log n)`. The modulus `q = 8380417` is specifically chosen so the NTT has a length-256 transform with simple twiddle factors.\n\nNone of these change the security argument or the high-level structure.\n\n### Hybrid deployment — Ed25519 + Dilithium together\n\nLike Kyber, the industry isn't betting the farm on a single post-quantum primitive that's only ~10 years old. The deployment pattern for signatures is the same: **run Ed25519 and Dilithium in parallel**, derive two signatures over the same message, and accept the message only if **both** signatures verify.\n\n```\nclient: send Ed25519_sig || Dilithium_sig\nserver: verify(Ed25519_sig) && verify(Dilithium_sig)\n```\n\nThe combined construction is safe as long as **either** primitive holds:\n\n- If Dilithium turns out to have a classical break we don't know about → Ed25519 still authenticates.\n- If quantum computers arrive → Dilithium still authenticates.\n\nDeployed since:\n\n- **OpenSSH 9.9** (October 2024) — `sntrup761x25519-sha512` and `mlkem768x25519-sha256` for KEX; ML-DSA tracking for host-key signatures.\n- **Cloudflare** — experimental Dilithium TLS certificates since 2024.\n- **CA/Browser Forum** — ballot process for ML-DSA in public-trust certificates; expected production rollout 2026-2027.\n- **AWS KMS** — ML-DSA-44/65/87 signing key support since 2024.\n- **Apple PQ3** (iMessage) — Kyber + Dilithium-class hybrid signatures on Apple's published roadmap.\n\n### Forward-link: SPHINCS+\n\nThe third NIST PQ signature standard is **SPHINCS+** (FIPS 205, also Aug 2024). It's hash-based — security relies only on the underlying hash function (no lattice assumption). Trade-off: signatures are ~8-50 KB depending on parameter set, signing is ~1000× slower than Dilithium. Used as the conservative fallback when lattice cryptanalysis worries you. A future lesson here will cover it.",
      "help_template": "**In plain English:** Real Dilithium comes in three sizes (44/65/87 by security level). Signatures are 2-5 KB — much bigger than Ed25519's 64 bytes, but that's the post-quantum tax. Deployments hybrid it with Ed25519 so the connection is safe even if one primitive breaks. OpenSSH, Cloudflare, AWS KMS already shipping; CA/Browser Forum rolling out 2026-2027.",
      "validator_key": "info", "codegen_key": "real_dilithium_and_deployment"
    }
  },
  {
    "model": "core.step",
    "pk": 258,
    "fields": {
      "lesson": 25, "order": 8, "slug": "done", "kind": "info",
      "prompt_template": "## Done!\n\nYou've built a working ML-DSA from the lattice up:\n\n- **The hard problem** — Module-SIS: find a short vector `z` such that `A·z = 0 mod q`. Believed hard for both classical and quantum computers at Dilithium's parameter sizes. Pairs with Kyber's Module-LWE.\n- **Toy keygen** — sample uniform `A`, two small secrets `s1, s2`, compute `t = A·s1 + s2 mod 257`. Public key `(A, t)`, private key `(s1, s2)`.\n- **Toy signing** — Fiat-Shamir loop: sample `y` short, compute `w = A·y`, derive `c = H(m, w)` ternary, compute `z = y + c·s1`, reject if `|z|_∞ > 36`, else output `(z, c, w)`. Real Dilithium does the same shape with bigger numbers and a sparse challenge.\n- **Toy verification** — check `|z|_∞ ≤ 36`, compute `w' = A·z − c·t`, check `|w − w'|_∞ ≤ 4` (the noise budget), check `H(m, w) == c`. The algebra closes because `A·z − c·t = w − c·s2`, with the big `c·A·s1` terms cancelling exactly.\n- **In-browser sign/verify** — the rejection loop runs live; each signature uses fresh `y`. Sign the same message twice and the bytes differ — opposite of Ed25519's deterministic signatures.\n- **Real ML-DSA** — `q = 8380417`, `n = 256`, `(k, l) ∈ {(4,4), (6,5), (8,7)}` for ML-DSA-44/65/87. Sparse fixed-weight challenge, hint-based signatures (no full `w`), NTT-accelerated multiplication. Deployed as the PQ half of the hybrid Ed25519 + Dilithium signature pattern.\n\n### Where this primitive shows up — *right now*\n\n- **OpenSSH 9.9+** — ML-KEM-768 hybrid KEX shipping; ML-DSA host-key support tracking.\n- **Cloudflare** — experimental Dilithium TLS certificates since 2024.\n- **AWS KMS** — ML-DSA-44/65/87 signing key support.\n- **Apple PQ3** (iMessage) — Kyber + Dilithium-class hybrid roadmap.\n- **CA/Browser Forum** — public-trust ML-DSA certs in ballot; expected 2026-2027.\n- **HSM vendors** (Thales, Utimaco, Entrust) — FIPS 204 support shipping in firmware.\n\n### What we skipped, on purpose\n\n- **Sparse fixed-weight challenge.** Real Dilithium's `c` has exactly `τ` non-zero coefficients in `{−1, +1}`. The toy uses uniformly-ternary `c` for simplicity. Same security shape; the sparsity tightens `|c·s|_∞`.\n- **Hint-based signatures.** Real ML-DSA ships a ~70-byte `hint` instead of the full `w` (~4 KB). The toy ships `w` directly so you can see the cancellation explicit.\n- **HighBits / MakeHint / UseHint machinery.** The rounding scheme that makes the hint work. Crucial for bytes-efficiency in real deployments.\n- **Number-Theoretic Transform.** `O(n log n)` polynomial multiplication. Toy uses schoolbook `O(n²)` because `n = 4`.\n- **Module-SIS / Module-LWE reduction proofs.** Standard lattice cryptography; whole textbooks on the topic.\n- **Constant-time implementation.** Real Dilithium implementations carefully avoid leaking `s1, s2` through timing or cache side-channels.\n\n### Forward links\n\nYou now have two post-quantum primitives:\n\n- **Kyber / ML-KEM** (FIPS 203) — key encapsulation. Lattice-based (Module-LWE).\n- **Dilithium / ML-DSA** (FIPS 204) — signatures. Lattice-based (Module-SIS).\n\nThe third NIST PQ standard is **SPHINCS+ / SLH-DSA** (FIPS 205) — hash-based signatures. No lattice assumption; security relies only on the hash function. Much larger signatures (~8-50 KB), much slower signing (~1000× slower than Dilithium), but the simplest security argument in cryptography. A future lesson here will cover it.\n\n### Python equivalent\n\nThe full script above uses `dilithium-py`, a pure-Python ML-DSA implementation that follows FIPS 204 exactly. Same three operations you just walked, at the real parameters:\n\n```python\nfrom dilithium_py.ml_dsa import ML_DSA_65\npk, sk = ML_DSA_65.keygen()\nsignature = ML_DSA_65.sign(sk, message)\nassert ML_DSA_65.verify(pk, message, signature)\n```\n\nGo back to the [home page](/) to revisit any algorithm.",
      "help_template": "",
      "validator_key": "info", "codegen_key": "full_script"
    }
  }
]
